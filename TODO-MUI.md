# MUI Integration for Portfolio

## Plan
1. ✅ Install MUI packages
2. ✅ Update `src/main.jsx` - Add CssBaseline, ThemeProvider
3. ✅ Update `src/App.jsx` - Convert to MUI components (AppBar, Container, Typography, Grid, Card, Button, Box, Chip)
4. ✅ Create `src/theme.js` - Custom theme
5. Update `src/App.css` - Remove custom styles, use MUI sx/theme
6. Update `src/index.css` - MUI font import
7. Test
8. Complete

**Next: Fix 500 error - likely MUI missing (install needed)**

